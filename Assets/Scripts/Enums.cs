using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums : MonoBehaviour
{
    public enum Direction { Left, Up, Down, Right}
    public enum Tolerance { Perfect, Great, Good, Bad, Miss}
}
